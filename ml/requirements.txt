# Roneira AI HIFI — ML Backend
# FastAPI-based ML prediction service

# Core
# fastapi pinned >=0.141 to pull a patched starlette (>=0.46) - 0.115.x was
# capped to starlette<0.42, which carried multiple known CVEs (PYSEC-2026-161,
# -248, -249, -1941, -1942, -2280, -2281).
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.11.3
python-dotenv==1.2.2

# Data
# Do not pin below 0.2.51: older releases fetch via plain `requests`, which
# Yahoo now blocks on UA/TLS fingerprint (returns HTML -> "Expecting value:
# line 1 column 1"). 0.2.51+ routes through curl_cffi and impersonates a
# browser, which is what actually gets price data back.
yfinance>=0.2.66,<0.3.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.1.0

# ML Models
scikit-learn>=1.3.0,<2.0.0
xgboost>=1.7.0,<2.0.0

# Utilities
httpx==0.28.1
redis==5.2.1
apscheduler==3.11.0
openpyxl==3.1.5

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.24.0
